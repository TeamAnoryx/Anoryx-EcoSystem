# F-010 (ADR-0012 §3) — Docker secret files live here. NEVER commit them.
# Ignore everything in this directory except the docs, this ignore file, and the
# dev-secret generator scripts (code, not secrets — required to run the stack).
*
!.gitignore
!README.md
!gen-dev-secrets.sh
!gen-dev-secrets.ps1

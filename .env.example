# Anoryx fleet auth — long-lived Claude Code OAuth token (Max subscription).
# Mint with:  claude setup-token
# Copy this file to .env (gitignored) and paste the token. Never commit .env.
CLAUDE_CODE_OAUTH_TOKEN=

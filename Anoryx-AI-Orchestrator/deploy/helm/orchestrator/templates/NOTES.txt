Anoryx-AI-Orchestrator {{ .Chart.AppVersion }} has been deployed as release "{{ .Release.Name }}".

1. Check rollout status:
   kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "orchestrator.fullname" . }}

2. Watch the pods (a pre-upgrade migration Job runs first):
   kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}

3. Port-forward and probe locally:
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "orchestrator.fullname" . }} 8081:{{ .Values.service.port }}
   curl -fs http://localhost:8081/health

4. Logs:
   kubectl -n {{ .Release.Namespace }} logs -l app.kubernetes.io/instance={{ .Release.Name }} --tail=100

{{- if not .Values.envSecret }}
{{- if not .Values.createEnvSecret }}

WARNING: no `envSecret` is set and `createEnvSecret` is false. The Orchestrator
needs ORCH_INGEST_HMAC_SECRET and ORCH_ADMIN_TOKEN (and, in external mode, the
DB URLs). Create them with the helper (keeps secrets out of the repo):
   bash deploy/helm/gen-k8s-secret.sh {{ .Release.Namespace }} {{ .Release.Name }}
   helm upgrade {{ .Release.Name }} ... --set envSecret={{ include "orchestrator.envSecretName" . }}
Until then the ingest seam cannot start (ORCH_INGEST_HMAC_SECRET is fail-loud).
{{- end }}
{{- end }}
{{- if .Values.postgres.bundled }}

NOTE: bundled Postgres is DEV/DEMO-GRADE. For production, set
postgres.bundled=false and point at a managed service (see deploy/DEPLOY-K8s.md).
{{- end }}

NOTE: mTLS between Sentinel and this ingest seam is NOT provisioned by this
chart — the interim peer authenticator is the shared ORCH_INGEST_HMAC_SECRET
(ADR-0008 §Honest deferrals, mirroring Anoryx-Sentinel's own F-010 deferral of
Vault/mTLS to later hardening).

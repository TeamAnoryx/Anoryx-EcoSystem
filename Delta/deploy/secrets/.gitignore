# D-010 — Docker secret files live here. NEVER commit them.
# Ignore everything in this directory except the docs and the dev-secret
# generator script (code, not secrets — required to run the stack).
*
!.gitignore
!README.md
!gen-dev-secrets.sh
